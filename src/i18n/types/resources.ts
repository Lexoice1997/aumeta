import app from '../uz/app.json';
import profile from '../uz/profile.json';
import main from '../uz/main.json';
import auth from '../uz/auth.json';

const resources = {
	app,
	profile,
	main,
	auth,
} as const;

export default resources;
